
- Install Neovim
```zsh
sudo apt update && sudo apt upgrade -y
sudo apt install neovim -y
nvim --version
```

- Structure
```text
~/.config
	/nvim
		/lua
			/config
			/plugins
			/utils
```

- `.config/nvim`
```shell
$ touch init.lua
```

```lua
require("config")
```

